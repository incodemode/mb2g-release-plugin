version: 2.1

orbs:
  maven: circleci/maven@1.0.1

workflows:
  maven_test:
    jobs:
      - maven/test:
          #command: '-X deploy -s .circleci/settings.xml'
          command: '-X --batch-mode release:prepare release:perform -s .circleci/settings.xml'
          pre-steps:
            - run: git config --global user.email "lmdbluis@gmail.com"
            - run: git config --global user.name "Via circleci build"